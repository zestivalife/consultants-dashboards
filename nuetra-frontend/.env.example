# API base URL for SSR and local tooling.
# Browser requests in production use the app's own /api/gateway proxy route.
# Production:
NEXT_PUBLIC_API_URL=https://api.nuetra.in/
GATEWAY_UPSTREAM_URL=https://api.nuetra.in
# Local dev (override in .env.local):
# NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
